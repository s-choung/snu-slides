# SKKU 발표자료 디자인 시스템 — 실측 디자인 토큰

> **이 문서는 `skku` 테마(navy/gold 레퍼런스 덱)의 단일 진실원천(SSOT)이다.** 기계가 읽는 토큰은
> `themes/skku.json`(+`skku_components.xml`), 이 문서는 그 근거·치수·컴포넌트 레시피를 담는다.
>
> **멀티스쿨**: 이 스킬은 skku / yu(영남대) / jnu(전남대) 3개 테마를 가진다. 공식 CI(색·로고 URL·폰트)는
> 각 테마 json의 `official_ci` 블록에 있다. **아래 §1~§7의 navy/gold 실측 hex(#0F0F70/#C5A86F 등)는
> 컴포넌트 GEOMETRY의 출처일 뿐, 출력 색이 아니다.** export 시 `clone.recolor`가 그 hex를 활성 테마색으로
> 리매핑한다(skku 출력색 = 공식 Navy #072B61 + Sungkyun Green #8DC63F). 형태·치수는 그대로, 색만 테마 지배.
> yu/jnu는 클론 라이브러리가 없어 primitive로 그린다.
> 새 학교 테마는 `themes/<school>.json`을 복제·수정하고 이 문서에 절을 추가한다.
>
> **테마 ↔ 토큰 매핑 (themes/snu.json)**
> - `colors`: navy `#0F0F70` · gold `#C5A86F` · gray `#888888` · accent_orange `#FA9028`(하단 띠 키워드) · ink `#1A1A1A` · banner_g1 `#DADCE2`.
> - `gradients`: numblock(45deg) · cover(표지 회색→navy 135deg) · banner(콘텐츠 상단 연회색 띠) · timeline.
> - `type_pt`: 제목24 · 본문14 · 캡션10 · 번호40 · 표지32. `box`: radius 11 · shadow `0 4px 9.3px rgba(0,0,0,.4)` · 카드테두리 3px navy.
> - `layout`: 1280×720 · 좌우마진42 · 배너84 · 본문시작112 · 하단띠60.
>
> **★회색 배너 (사용자 요청 — 명시)**: 원본 *콘텐츠* 슬라이드 상단은 흰 배경 + 번호블록/칩/제목뿐, 회색 바 **없음**.
> 회색 그라데는 **표지·섹션 풀블리드 배경**(135deg `#DADCE2`→`#B9BACF`→navy, §2-5)에서 옴.
> 제너레이터는 SNU 톤과 일관되게 콘텐츠 상단에 **연회색 헤더 띠**(`gradients.banner`)를 기본 적용한다. 끄려면 `gradients.banner`를 흰색으로.

---

> 출처: `/tmp/ppt_full/ppt/slides/slide1~30.xml` + 렌더 이미지 `full_s01~s30.png` 실측.
> 단위: 1280×720px 슬라이드(12192000×6858000 EMU). EMU→px = ÷9525, pt→px = ×96/72.
> **그라데이션 각도 규칙**: CSS각도 = `PowerPoint ang/60000 + 90` (mod 360). 아래 표의 "CSS deg"는 변환 완료값.
> 테마(theme1.xml)는 **기본 Office 테마** — navy/gold는 schemeClr가 아니라 도형마다 `srgbClr`로 직접 박혀 있음. clrMap: bg1=lt1(white), tx1=dk1(black), accent1=#4472C4(미사용에 가까움).

> ⚠ **가장 중요한 구조적 사실**: 상단 배너·번호블록·섹션칩·하단 띠는 **마스터/레이아웃에 없다**. master는 PowerPoint 기본 placeholder만 가지고 있고, 배너 구성요소는 **각 콘텐츠 슬라이드에 매번 그려 넣은 free-form 도형들**이다. 새 슬라이드를 찍으려면 아래 좌표대로 도형을 직접 배치해야 함.

---

## 1. 색 팔레트 + 역할

### 1-1. 핵심 브랜드 색 (빈도 = 슬라이드 1~30 전체 run+shape fill 카운트)

| HEX | 빈도(text/fill) | 역할 |
|---|---|---|
| `#0F0F70` navy | text 3 / **fill 36** | **메인 navy.** 번호블록, 카드 헤더바, 하단 강조 띠, 카드 테두리, 강조 텍스트 |
| `#C5A86F` gold | **text 44** / **fill 50** | **메인 gold.** 섹션칩, 강조 키워드(일부), gold 카드 헤더/테두리, gold 그라데 끝색 |
| `#161570` deep-navy | text 12 / fill 17 | navy 변형(번호블록 그라데 끝색, 일부 헤더). 거의 navy와 동급 |
| `#888888` gray | text 19 / fill 19 | **회색 톤 1.** "Understand"류 중립 카드 헤더·테두리, 보조 라벨 |
| `#FFFFFF` / `scheme:bg1` | fill 170+ | 카드 배경(흰색). 가장 빈번한 fill |
| `#1A1A1A` near-black | text 47 | 진한 본문/캡션용 검정 |
| `scheme:tx1` (=black) | **text 313** | **기본 본문 색** (제목·본문 다수가 명시색 없이 tx1 상속) |
| `#FA9028` orange | text 16 | **하단 강조 띠의 키워드 색**(주황). "expands", "scientific discovery" 등 |
| `#E59401` amber | text 17 / fill 3 | 주황-계열 강조 변형(아이콘/하이라이트) |
| `#840032` maroon | text 23 / fill 4 | 보조 강조(짙은 적자색). 일부 박스 20% 배경틴트 |
| `#00B0F0` cyan | text 11 / fill 14 | 보조 강조(밝은 파랑), 다이어그램 포인트 |
| `#FF0000` red | text 12 | "[Editors' Highlight]" 등 빨강 강조 라벨 |
| `#04223F`/`#002542`/`#0D273A` | text 14 / fill | navy보다 더 어두운 청록-네이비(다이어그램 배경) |
| `#4476A3` steel-blue | text 8 | 보조 캡션/링크풍 |

### 1-2. 역할 → 색 매핑(권장 토큰)

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--navy` | `#0F0F70` | 제목 강조·번호블록·카드헤더·하단띠·테두리 |
| `--navy-deep` | `#161570` | navy 그라데 끝, 일부 헤더 |
| `--gold` | `#C5A86F` | 섹션칩, gold 카드, 강조 키워드 |
| `--gray` | `#888888` | 중립 카드/라벨/구분선 |
| `--ink` | `#1A1A1A`~`tx1(#000)` | 본문 |
| `--accent-orange` | `#FA9028` | 하단 띠 키워드 강조 |
| `--accent-cyan` | `#00B0F0` | 다이어그램 포인트 |
| `--card-bg` | `#FFFFFF` | 카드 배경 |
| `--panel-tint` | 위 색 + `alpha 20%` | 연한 박스 배경(아래 4번) |

> **알파-틴트 패턴**: 박스 배경에 원색을 그대로 안 쓰고 `srgbClr + <a:alpha val="20000">`(=20%)로 흐리게 깐다. 예: gold칩 헤더 탭 `#C5A86F a20`, navy 탭 `#0F0F70 a20`, gray 탭 `#888888 a20`, maroon 박스 `#840032 a20`, amber `#E59401 a34`.

---

## 2. 상단 배너(헤더) 구조 ★중요

콘텐츠 슬라이드(예 slide3/5/9) 상단은 **4개 독립 도형**으로 구성. 모두 슬라이드에 직접 그려짐.

### 2-1. 좌상단 번호블록 (navy 정사각 그라데)
| 항목 | 값 |
|---|---|
| 도형 | `rect` (정사각형, 모서리 직각) |
| 위치/크기 | **@(0, 0)px, 108.3 × 83.7px** (EMU off 0,0 / ext 1031875×797223) |
| 채움 | **gradFill, CSS 45deg**, stops: `#060634 @11%` → `#0F0F70 @56%` (PowerPoint ang=315000) |
| 텍스트 | "01"/"02"… sz=**39.99pt**, **bold**, Arial, 자간 spc=300 |
| 텍스트 색 | `prstClr white + alpha 30%` = **반투명 흰색**(번호가 블록에 살짝 묻힘) + 외곽선 `ln w=9525(=1px)` 투명 |
| 텍스트 박스 | @(6.3, 4.7)px 97.1×74.3, 가운데정렬 |

### 2-2. 섹션 라벨 (번호블록 우측 상단 작은 회색 텍스트)
| 항목 | 값 |
|---|---|
| 위치 | @(129, 0)px, w≈294~329px(내용따라), h=38.8px |
| 텍스트 | "Introduction" / "Research Overview" 등 |
| 폰트/색 | Arial **bold**, 색 = `prstClr black + lumMod50% + lumOff50%` = **#808080 회색**. sz 미지정(상속, 대략 14pt 렌더) |

### 2-3. 섹션칩 (gold pill, 라벨 바로 아래)
| 항목 | 값 |
|---|---|
| 도형 | `roundRect`, **adj=50000 (완전 알약형)**, r≈15px |
| 위치/크기 | **@(129, 39.8)px, 64.2 × 30.2px** (ext 611968×287985) |
| 채움 | solidFill `#C5A86F` (gold), 테두리 noFill |
| 텍스트 | "1.1","1.2","2.1"… Arial, 가운데정렬, **색 = white**(fontRef minor→lt1 상속) |

### 2-4. 슬라이드 제목 (칩 우측, 큰 텍스트)
| 항목 | 값 |
|---|---|
| 위치 | @(206.3, 26.7)px, w≈768~969px, h=52.3px |
| 폰트 | Arial, **sz=24pt**, regular(bold 아님), 색 = tx1(검정) 상속, anchor=ctr |

### 2-5. 회색 그라데이션 레이어 — **타이틀/섹션 슬라이드 배경** ★
이 회색 그라데는 콘텐츠 배너가 아니라 **표지(slide1)·섹션표지·일부 풀블리드 배경**의 전면 그라데다.

| 슬라이드 | 도형/위치 | CSS각도 | 그라데 스톱 (정렬·CSS순) |
|---|---|---|---|
| **slide1 (표지)** | rect @(-16,-13) 1296×747 (풀블리드) | **135deg** | `#DADCE2 a0% @21%` → `#B9BACF a7% @41%` → `#0F0F70 a56% @0%`→`#0F0F70 a20.8% @100%` *(navy 코너 글로우 + 회색 띠)* |
| **slide5 (풀배경)** | rect @(0,90) 1280×630 | **135deg** | `bg1(흰) a51% @0%` → `#C5A86F a15% @49%` → `#0F0F70 a56% @100%` |

> 작업지시서가 언급한 `linear-gradient(90deg, #DADCE2 21%, …)` 류의 **연한 회색 띠**는 정확히 표지의 `#DADCE2`(21%)·`#B9BACF`(41%) 스톱에서 나온 것. 실측 CSS 각도는 **135deg**(PowerPoint ang=45°). 회색→navy로 넘어가는 대각선 그라데.

### 2-6. 콘텐츠 슬라이드 본문 배경
콘텐츠 슬라이드(slide3/9 등)는 상단 배너 영역에 **회색 그라데 바가 없다**(렌더상 흰 배경 + 번호블록/칩/제목만). 회색 그라데는 표지·섹션·풀블리드형에서만.

---

## 3. 타입 스케일 (slide1~30 전체 run sz 히스토그램)

| pt | px | 빈도 | bold빈도 | 역할(실측) |
|---|---|---|---|---|
| **39.99** | 53.3 | 52 | 52(전부 bold) | 번호블록 "01" 숫자 |
| 32 | 42.7 | 4 | 2 | 표지 메인 타이틀 (slide1) |
| 24 | 32 | 52 | 19 | **슬라이드 제목**(배너), 표지 발표자명, 하단띠 강조키워드 |
| 20 | 26.7 | 62 | 34 | 부제/소제목, 표지 소속 |
| 18 | 24 | 27 | 8 | 표지 날짜, 큰 본문 |
| 16 | 21.3 | 108 | 24 | 카드 헤더 텍스트, 표지 한글부제, 중제목 |
| **14** | 18.7 | **279(최다)** | 44 | **기본 본문/카드 본문** + 카드 탭 라벨 |
| 12 | 16 | 219 | 9 | 본문 보조, 도식 라벨, 캡션 상위 |
| 11 | 14.7 | 23 | 2 | 작은 라벨 |
| 10.5 | 14 | 33 | 2 | 작은 캡션/이미지 라벨 |
| **10** | 13.3 | 188 | 22 | **출처/인용 라인**(S. Choung et al. …), 작은 라벨 |
| 9.5 / 9 | 12.7/12 | 14/83 | | 미세 캡션·각주 |
| 8 / 7 / 6 / 5.5 | | 16/19/36/42 | 0 | 도식 내 초미세 라벨·아래첨자 |

**역할별 대표값(권장)**
| 역할 | pt | 굵기 | 폰트 | 색 |
|---|---|---|---|---|
| 표지 타이틀 | 32 | bold | Arial | tx1 |
| 슬라이드 제목(배너) | 24 | regular | Arial | tx1(검정) |
| 번호블록 숫자 | 40 | bold | Arial | white a30% |
| 섹션 라벨 | ~14 | bold | Arial | #808080 |
| 카드 헤더 | 16 | (bg에 따라) | Arial | white(navy/gold bg 위) |
| 부제/소제목 | 20 | bold | Arial | navy/tx1 |
| 본문 | **14** | regular | Arial | tx1 / #1A1A1A |
| 보조 라벨 | 12 | regular | Arial | tx1 |
| 캡션/출처 | 10 | reg(이름만 bold) | Arial | tx1, 강조어 #FF0000 |
| 하단 띠 키워드 | 24 | bold | Arial | #FA9028 |

**폰트**: 라틴 **Arial**(압도적, run 1244회), 한글 **맑은 고딕**(348회). 일부 표지에 **나눔스퀘어** 1회. major/minor 폰트 모두 Arial+맑은고딕.

---

## 4. 박스/카드 규격

### 4-1. roundRect radius (adj→px, 빈출 순)
| adj | 반지름 | 대표 박스(px) | 용도 |
|---|---|---|---|
| **50000** | =짧은변/2 (완전 pill) | 64×30 (r15), 127×54 (r27) | **섹션칩**, 알약 라벨 |
| **16667**(PPT기본) | ≈짧은변×0.167 | 539×57(r9.4), 542×31(r5.2) | 일반 라운드 라벨/바 |
| **19768** | r 8~10.6 | 149×41, 353×54 | 카드 탭/헤더 바(둥근) |
| **2939~4698** | r 2.3~11(큰 박스) | 377×495(r11), 501×66(r3) | **큰 카드 패널**(모서리 살짝) |
| **5954~8358** | r 2~8 | 142×38, 96×123 | 작은 아이콘/이미지 프레임 |

> 실무 요약: **칩 = adj 50000(pill)**, **카드 헤더바 = adj ~16667~19768(r≈8~10px)**, **큰 카드 = 절대반경 r≈11px**(adj가 작아짐).

### 4-2. 텍스트 inset(패딩) — bodyPr lIns/tIns/rIns/bIns
| 패턴 | 값(px) | 빈도 | 비고 |
|---|---|---|---|
| zero-inset | l/r=0 (46회), t/b=0 | 최다 | 라벨류 패딩 제거 |
| 기본 | l/r=**9.4**, t/b=**4.7** | 7 | PowerPoint 기본(91440/45720 EMU) |
| 넓은 카드 | l=26.5, t=15.1, r=18.9 | 5 | 본문 카드 |
| 넓은 패널 | l=56.7, r=30.2 | 2 | 큰 패널 |

### 4-3. 그림자(outerShdw)
| 패턴 | blur | dist | dir | color/alpha | 빈도 |
|---|---|---|---|---|---|
| **표준 카드/바 그림자** | 9.3px | 4.0px | 0° | (검정 기본)| **24(최빈)** |
| 부드러운 | 6.7px | 0 | 0° | | 4 |
| 대각 | 5.3px | 4px | 45° | | 3 |
| 헤더바 텍스트 그림자 | 38100EMU=4px blur, dist 4px | dir=2700000(45°) | #000 a43% | 카드헤더 글자 |
| inner-glow류 | 21.3px | 1.6px | 0° | bg1(흰) a89% | 2 |

> CSS 환산 예(표준): `box-shadow: 0 4px 9.3px rgba(0,0,0,~0.4)` (테마 effectStyle 기본 alpha 63%; 도형별 미지정시 검정).

### 4-4. 자주 쓰는 박스 2~3종
1. **큰 콘텐츠 카드**: roundRect, 흰 배경(bg1), 테두리 **3px**(=ln w 28575) navy `#0F0F70`(또는 gold/gray), r≈11px, 그림자 표준. 예 slide9 @(16.8,169) 618×508, slide5 376×495.
2. **카드 헤더 바**: roundRect, 채움 `#0F0F70`(navy) 또는 gold/gray, r≈8px, 텍스트 white 16pt + 글자 drop-shadow. 예 slide9 @(64,121.7) 524×43.5.
3. **연한 틴트 칩/탭**: roundRect, 채움 = 원색 + alpha 20%, 테두리 없음, 라벨 텍스트. 예 헤더 탭 `#888888 a20` 259×39.

---

## 5. 좌상단 번호 블록 + 섹션칩 (2-1·2-3 재정리, 정량)

| 컴포넌트 | 도형 | 위치(px) | 크기(px) | 채움 | 텍스트 |
|---|---|---|---|---|---|
| **번호블록** | rect(직각) | (0, 0) | 108.3 × 83.7 | grad 45deg `#060634`→`#0F0F70` | "01" 40pt bold, white a30% |
| **섹션 라벨** | text | (129, 0) | ~300 × 38.8 | 없음 | bold #808080, ~14pt |
| **섹션칩** | roundRect adj50000 | (129, 39.8) | 64.2 × 30.2 | `#C5A86F` solid | "1.2" white, 가운데 |
| **제목** | text | (206.3, 26.7) | ~900 × 52.3 | 없음 | 24pt Arial tx1 |

칩-라벨은 **좌측 정렬 동일 x=129px**로 번호블록 우측에 세로로 라벨(위)/칩(아래) 스택. 칩 높이≈30px, gold pill.

---

## 6. 레이아웃 그리드 / 여백

| 항목 | 값 |
|---|---|
| 슬라이드 | 1280 × 720px |
| 번호블록 | 좌상단 (0,0), 108×84px 점유 |
| 배너 영역 높이 | ~0~84px(번호블록), 제목 baseline ~80px |
| **본문 시작 y** | 카드형은 **y≈87~137px**부터(예 slide9 큰 카드 y=86.9 / 169) |
| 좌우 마진 | 카드 좌측 시작 x≈**16~42px**, 우측 끝 x≈1262px (좌우 ~16~40px 여백) |
| **3단 카드 그리드**(slide5) | 카드 폭 **376.6px**, 카드 x = 41.7 / 447 / 852.2 → **간격 ≈28.7px(=405−376.6 사이 gap)**, 카드 높이 495px, top y=136.9 |
| 2단 그리드(slide9) | 좌 카드 x=16.8 w=618.7, 우 카드 x=476.8 w=785.4 (비대칭 폭) |
| 2×2 그리드(slide7) | 4분할, 각 카드에 navy 헤더바 + 콘텐츠 |
| **하단 강조 띠**(footer band) | rect 풀폭 1280px, y≈**652~665px**, 높이 **55~68px**, 채움 `#0F0F70`. 텍스트 24pt bold white, 키워드 `#FA9028` |
| 출처 라인 footer | 우하단 @(~650,693) w≈592 h≈26, 10pt, "S. Choung† et al. …" |

---

## 7. 컴포넌트 인벤토리

| 컴포넌트 | 출현 | 대표 파라미터 |
|---|---|---|
| **배너 헤더 세트** | 모든 콘텐츠 슬라이드 | 번호블록(0,0,108×84,grad navy) + 라벨(#808080) + gold칩(pill 64×30) + 제목(24pt) |
| **번호블록** | 콘텐츠 전부 | rect 108×84, grad 45deg #060634→#0F0F70, "0N" 40pt bold white-a30 |
| **섹션칩** | 콘텐츠 전부 | roundRect pill(adj50000) 64×30, #C5A86F, white text |
| **큰 콘텐츠 카드** | slide5/9/7 등 | roundRect 흰배경 + 3px navy/gold/gray 테두리 + r≈11 + 표준 그림자 |
| **카드 헤더 바** | slide7/9 | roundRect navy(#0F0F70) r≈8, white 16pt + 글자그림자 |
| **틴트 탭/칩 라벨** | slide5/9 | roundRect, 원색 a20%, 라벨 |
| **3단 비교 카드** | slide5 | 폭376px×3, gap≈29px, 헤더 색으로 단계 구분(gray/gold/navy = Understand/Design/Accelerate) |
| **2×2 비교 그리드** | slide7 | 4 카드 + navy 헤더바, 출처라인, 인물사진 |
| **타임라인/화살표** | slide3 | 4-paradigm 가로 화살표(rightArrow류), 그라데 #C5A86F→#0F0F70(CSS 270deg, ang=180), 각 단계 라벨칩 |
| **하단 강조 띠** | slide3/5 등 | 풀폭 navy rect y≈655, h≈55~68, 24pt bold white + #FA9028 키워드 |
| **출처/인용 라인** | 데이터 슬라이드 | 10pt, 이름 bold, "[Editors' Highlight]" #FF0000 |
| **표지 그라데 배경** | slide1 | 풀블리드 rect, 135deg, #DADCE2/#B9BACF 회색→navy 글로우 |
| **섹션 전환 그라데** | slide2/5 | navy→gold 세로/대각 그라데(ang 0/45), 알파 60~100% |
| **다이어그램/원자구조 이미지** | slide5/9/11 | bg1 ellipse/roundRect 프레임, cyan-teal 그라데 노드(#10A5C8→#3AB59D) |

---

### 부록: 자주 쓰는 그라데이션 레시피
| 이름 | PPT ang | CSS deg | 스톱 |
|---|---|---|---|
| 번호블록 navy | 315000 | 45 | #060634 @11% → #0F0F70 @56% |
| 타임라인 화살표 | 180 | 270 | #C5A86F @0% → #0F0F70 @100% |
| 표지 회색→navy | 45 | 135 | #DADCE2 a0 @21% → #B9BACF a7 @41% → navy glow |
| 섹션 navy→gold | 0 | 90 | #161570 a71% → #C5A86F a31% |
| 다이어그램 노드 teal | 0/250 | 90/340 | #10A5C8 → #3AB59D |

> 불명/주의: 콘텐츠 슬라이드 일반 배경은 단색 흰색(bg1)으로 보이며 별도 회색 배너 바는 미관측. 회색 그라데는 표지·풀블리드 전용. 마스터에는 배너 요소 전무(전부 슬라이드 직접 작도).
